export const ANNOTATIONS = {
  KEY: "@cds.vector.key",
  TYPE: "@cds.vector.type",
};

export enum SUPPORTED_TYPES {
  date = "date",
  numeric = "numeric",
  category = "category",
  unfold = "unfold",
}
